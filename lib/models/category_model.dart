class CategoryModel {
  String? categoryName;
  String? image;
  String? apiEndpoint; // Add this field for storing the API endpoint

  CategoryModel({this.categoryName, this.image, this.apiEndpoint});
}
